diff --git a/checkbox/root/CheckboxRoot.js b/checkbox/root/CheckboxRoot.js
index 7000305591bd64e9f26ed573e12ee41a873cafe4..52cfbef7a269d4abcdfe8311b42269750ee15529 100644
--- a/checkbox/root/CheckboxRoot.js
+++ b/checkbox/root/CheckboxRoot.js
@@ -7,6 +7,7 @@ Object.defineProperty(exports, "__esModule", {
 });
 exports.PARENT_CHECKBOX = exports.CheckboxRoot = void 0;
 var React = _interopRequireWildcard(require("react"));
+var _useStableCallback = require("@base-ui/utils/useStableCallback");
 var _empty = require("@base-ui/utils/empty");
 var _useControlled = require("@base-ui/utils/useControlled");
 var _useIsoLayoutEffect = require("@base-ui/utils/useIsoLayoutEffect");
@@ -139,6 +140,37 @@ const CheckboxRoot = exports.CheckboxRoot = /*#__PURE__*/React.forwardRef(functi
   (0, _useRegisterFieldControl.useRegisterFieldControl)(controlRef, id, checked, undefined, !groupContext && !disabled, nameProp);
   const registerChildId = parentContext?.registerChildId;
   const inputRef = React.useRef(null);
+  // Native reset emits no change event; keep the existing state owner authoritative.
+  const resetInput = (0, _useStableCallback.useStableCallback)(() => {
+    if (groupChecked !== undefined || groupValue !== undefined) {
+      if (inputRef.current) inputRef.current.checked = checked;
+    } else {
+      setCheckedState(defaultChecked);
+    }
+  });
+  React.useEffect(() => {
+    const formElement = inputRef.current?.form;
+    if (!formElement) {
+      return undefined;
+    }
+    let active = true;
+    const pendingResets = new Set();
+    function handleReset(event) {
+      // A native reset's default action can follow microtask checkpoints.
+      // The next task observes completed reset and ancestor cancellation.
+      const task = setTimeout(() => {
+        pendingResets.delete(task);
+        if (active && !event.defaultPrevented) resetInput();
+      }, 0);
+      pendingResets.add(task);
+    }
+    formElement.addEventListener('reset', handleReset);
+    return () => {
+      active = false;
+      pendingResets.forEach(clearTimeout);
+      formElement.removeEventListener('reset', handleReset);
+    };
+  }, [form, resetInput]);
   const registerFieldInput = validation.registerInput;
   const registeredInputValue = groupContext ? value : undefined;
   const registerInput = React.useCallback(element => registerFieldInput(element, {
diff --git a/checkbox/root/CheckboxRoot.mjs b/checkbox/root/CheckboxRoot.mjs
index 8276fca70dc1a847bce9cf5a7c46207a2b1da49b..d52904681ab7f8f97ede30bd7e9723c83d93fdff 100644
--- a/checkbox/root/CheckboxRoot.mjs
+++ b/checkbox/root/CheckboxRoot.mjs
@@ -3,4 +3,5 @@
 import * as React from 'react';
+import { useStableCallback } from '@base-ui/utils/useStableCallback';
 import { EMPTY_OBJECT } from '@base-ui/utils/empty';
 import { useControlled } from '@base-ui/utils/useControlled';
 import { useIsoLayoutEffect } from '@base-ui/utils/useIsoLayoutEffect';
@@ -133,6 +134,37 @@ export const CheckboxRoot = /*#__PURE__*/React.forwardRef(function CheckboxRoot(
   useRegisterFieldControl(controlRef, id, checked, undefined, !groupContext && !disabled, nameProp);
   const registerChildId = parentContext?.registerChildId;
   const inputRef = React.useRef(null);
+  // Native reset emits no change event; keep the existing state owner authoritative.
+  const resetInput = useStableCallback(() => {
+    if (groupChecked !== undefined || groupValue !== undefined) {
+      if (inputRef.current) inputRef.current.checked = checked;
+    } else {
+      setCheckedState(defaultChecked);
+    }
+  });
+  React.useEffect(() => {
+    const formElement = inputRef.current?.form;
+    if (!formElement) {
+      return undefined;
+    }
+    let active = true;
+    const pendingResets = new Set();
+    function handleReset(event) {
+      // A native reset's default action can follow microtask checkpoints.
+      // The next task observes completed reset and ancestor cancellation.
+      const task = setTimeout(() => {
+        pendingResets.delete(task);
+        if (active && !event.defaultPrevented) resetInput();
+      }, 0);
+      pendingResets.add(task);
+    }
+    formElement.addEventListener('reset', handleReset);
+    return () => {
+      active = false;
+      pendingResets.forEach(clearTimeout);
+      formElement.removeEventListener('reset', handleReset);
+    };
+  }, [form, resetInput]);
   const registerFieldInput = validation.registerInput;
   const registeredInputValue = groupContext ? value : undefined;
   const registerInput = React.useCallback(element => registerFieldInput(element, {
diff --git a/radio/root/RadioRoot.js b/radio/root/RadioRoot.js
index a8768749f2ac1f64d25ea029b54888ae5f34a61c..ca85d6c0a991b4f86deb04ee36ca1a37c2e477a9 100644
--- a/radio/root/RadioRoot.js
+++ b/radio/root/RadioRoot.js
@@ -7,6 +7,7 @@ Object.defineProperty(exports, "__esModule", {
 });
 exports.RadioRoot = void 0;
 var React = _interopRequireWildcard(require("react"));
+var _useStableCallback = require("@base-ui/utils/useStableCallback");
 var _useMergedRefs = require("@base-ui/utils/useMergedRefs");
 var _useIsoLayoutEffect = require("@base-ui/utils/useIsoLayoutEffect");
 var _visuallyHidden = require("@base-ui/utils/visuallyHidden");
@@ -83,6 +84,36 @@ const RadioRoot = exports.RadioRoot = /*#__PURE__*/React.forwardRef(function Rad
   const checked = groupContext ? checkedValue === value : value === '';
   const radioRef = React.useRef(null);
   const inputRef = React.useRef(null);
+  // Native reset emits no change event; keep the existing state owner authoritative.
+  const resetInput = (0, _useStableCallback.useStableCallback)(() => {
+    groupContext?.resetValue();
+    if (groupContext?.controlled && inputRef.current) {
+      inputRef.current.checked = checked;
+    }
+  });
+  React.useEffect(() => {
+    const formElement = inputRef.current?.form;
+    if (!formElement) {
+      return undefined;
+    }
+    let active = true;
+    const pendingResets = new Set();
+    function handleReset(event) {
+      // A native reset's default action can follow microtask checkpoints.
+      // The next task observes completed reset and ancestor cancellation.
+      const task = setTimeout(() => {
+        pendingResets.delete(task);
+        if (active && !event.defaultPrevented) resetInput();
+      }, 0);
+      pendingResets.add(task);
+    }
+    formElement.addEventListener('reset', handleReset);
+    return () => {
+      active = false;
+      pendingResets.forEach(clearTimeout);
+      formElement.removeEventListener('reset', handleReset);
+    };
+  }, [form, resetInput]);
   const registerFieldInput = validation?.registerInput;
   const registerInput = React.useCallback(element => registerFieldInput?.(element, {
     controlRef: radioRef,
diff --git a/radio/root/RadioRoot.mjs b/radio/root/RadioRoot.mjs
index 4e8ca6b6dc0831b80f7164cb22544dc76f8651aa..e9dc619dccab31872b1897515dc3b120a39f6db6 100644
--- a/radio/root/RadioRoot.mjs
+++ b/radio/root/RadioRoot.mjs
@@ -3,4 +3,5 @@
 import * as React from 'react';
+import { useStableCallback } from '@base-ui/utils/useStableCallback';
 import { useMergedRefs } from '@base-ui/utils/useMergedRefs';
 import { useIsoLayoutEffect } from '@base-ui/utils/useIsoLayoutEffect';
 import { visuallyHidden, visuallyHiddenInput } from '@base-ui/utils/visuallyHidden';
@@ -78,6 +79,36 @@ export const RadioRoot = /*#__PURE__*/React.forwardRef(function RadioRoot(compon
   const checked = groupContext ? checkedValue === value : value === '';
   const radioRef = React.useRef(null);
   const inputRef = React.useRef(null);
+  // Native reset emits no change event; keep the existing state owner authoritative.
+  const resetInput = useStableCallback(() => {
+    groupContext?.resetValue();
+    if (groupContext?.controlled && inputRef.current) {
+      inputRef.current.checked = checked;
+    }
+  });
+  React.useEffect(() => {
+    const formElement = inputRef.current?.form;
+    if (!formElement) {
+      return undefined;
+    }
+    let active = true;
+    const pendingResets = new Set();
+    function handleReset(event) {
+      // A native reset's default action can follow microtask checkpoints.
+      // The next task observes completed reset and ancestor cancellation.
+      const task = setTimeout(() => {
+        pendingResets.delete(task);
+        if (active && !event.defaultPrevented) resetInput();
+      }, 0);
+      pendingResets.add(task);
+    }
+    formElement.addEventListener('reset', handleReset);
+    return () => {
+      active = false;
+      pendingResets.forEach(clearTimeout);
+      formElement.removeEventListener('reset', handleReset);
+    };
+  }, [form, resetInput]);
   const registerFieldInput = validation?.registerInput;
   const registerInput = React.useCallback(element => registerFieldInput?.(element, {
     controlRef: radioRef,
diff --git a/radio-group/RadioGroup.js b/radio-group/RadioGroup.js
index 28192b015befb7a8896a5056262145b924abd01d..3ca815ca1710c5b93c20c9a6b14e618fedb291ca 100644
--- a/radio-group/RadioGroup.js
+++ b/radio-group/RadioGroup.js
@@ -77,6 +77,10 @@ const RadioGroup = exports.RadioGroup = /*#__PURE__*/React.forwardRef(function R
     name: 'RadioGroup',
     state: 'value'
   });
+  const resetValue = (0, _useStableCallback.useStableCallback)(() => {
+    setCheckedValueUnwrapped(defaultValue);
+  });
+  const controlled = externalValue !== undefined;
   const [touched, setTouched] = React.useState(false);
   const setCheckedValue = (0, _useStableCallback.useStableCallback)((value, eventDetails) => {
     onValueChangeProp?.(value, eventDetails);
@@ -172,6 +176,8 @@ const RadioGroup = exports.RadioGroup = /*#__PURE__*/React.forwardRef(function R
   };
   const contextValue = React.useMemo(() => ({
     checkedValue,
+    controlled,
+    resetValue,
     disabled,
     form,
     validation,
@@ -182,7 +188,7 @@ const RadioGroup = exports.RadioGroup = /*#__PURE__*/React.forwardRef(function R
     setCheckedValue,
     setTouched,
     touched
-  }), [checkedValue, disabled, form, validation, name, readOnly, registerInputRef, required, setCheckedValue, setTouched, touched]);
+  }), [checkedValue, controlled, resetValue, disabled, form, validation, name, readOnly, registerInputRef, required, setCheckedValue, setTouched, touched]);
   const defaultProps = {
     id: idProp,
     role: 'radiogroup',
diff --git a/radio-group/RadioGroup.mjs b/radio-group/RadioGroup.mjs
index ef202bd782287091b2a7d0d179fe5d43c4ff73a3..34d5f2b811eb9412c071f3340c575d1438044a39 100644
--- a/radio-group/RadioGroup.mjs
+++ b/radio-group/RadioGroup.mjs
@@ -71,6 +71,10 @@ export const RadioGroup = /*#__PURE__*/React.forwardRef(function RadioGroup(comp
     name: 'RadioGroup',
     state: 'value'
   });
+  const resetValue = useStableCallback(() => {
+    setCheckedValueUnwrapped(defaultValue);
+  });
+  const controlled = externalValue !== undefined;
   const [touched, setTouched] = React.useState(false);
   const setCheckedValue = useStableCallback((value, eventDetails) => {
     onValueChangeProp?.(value, eventDetails);
@@ -166,6 +170,8 @@ export const RadioGroup = /*#__PURE__*/React.forwardRef(function RadioGroup(comp
   };
   const contextValue = React.useMemo(() => ({
     checkedValue,
+    controlled,
+    resetValue,
     disabled,
     form,
     validation,
@@ -176,7 +182,7 @@ export const RadioGroup = /*#__PURE__*/React.forwardRef(function RadioGroup(comp
     setCheckedValue,
     setTouched,
     touched
-  }), [checkedValue, disabled, form, validation, name, readOnly, registerInputRef, required, setCheckedValue, setTouched, touched]);
+  }), [checkedValue, controlled, resetValue, disabled, form, validation, name, readOnly, registerInputRef, required, setCheckedValue, setTouched, touched]);
   const defaultProps = {
     id: idProp,
     role: 'radiogroup',
